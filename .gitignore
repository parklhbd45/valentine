# --- Dependencies (ไม่ต้องเอาขึ้น เพราะมันเยอะมาก) ---
node_modules/
/node_modules

# --- Environment Variables (สำคัญมาก! ห้ามเอาขึ้น) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.env.*

# --- Logs (ไฟล์ Log error ต่างๆ) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# --- OS Files (ไฟล์ขยะของระบบปฏิบัติการ) ---
.DS_Store
Thumbs.db

# --- Vercel (ไฟล์ที่ Vercel สร้างตอนรัน local) ---
.vercel/

# --- IDE Settings (การตั้งค่าของ Editor ส่วนตัว) ---
.vscode/
.idea/